---
title: Project Structure - Yamify
---

# Project Structure

This page provides an overview of Yamify's project structure, explaining the purpose of each directory and the organization of the codebase.

## Root Directory

```
/
├── .git/                  # Git repository data
├── .github/               # GitHub specific files (workflows, templates)
├── .gitignore             # Git ignore rules
├── README.md              # Project overview and basic documentation
├── eslint.config.mjs      # ESLint configuration
├── next.config.ts         # Next.js configuration
├── node_modules/          # Node.js dependencies
├── package-lock.json      # Dependency lock file
├── package.json           # Project metadata and dependencies
├── postcss.config.mjs     # PostCSS configuration
├── public/                # Static assets
├── src/                   # Source code
└── tsconfig.json          # TypeScript configuration
```

## Source Directory (`/src`)

The `src` directory contains all the application source code, organized as follows:

```
src/
├── app/                  # Next.js App Router components and routes
├── assets/               # Non-public assets (SVGs, images used in components)
├── components/           # Reusable UI components
├── libs/                 # Utility libraries and external integrations
├── styles/               # Global styles and style utilities
└── utils/                # Helper functions and utilities
```

### App Directory (`/src/app`)

The `app` directory follows Next.js 13+ App Router structure:

```
app/
├── auth/                # Authentication related pages
├── dashboard/           # Dashboard related pages and components
├── favicon.ico          # Website favicon
├── globals.css          # Global CSS styles
├── layout.tsx           # Root layout component
└── page.tsx             # Homepage component
```

### Dashboard Directory (`/src/app/dashboard`)

The dashboard section contains:

```
dashboard/
├── _components/         # Dashboard-specific components
└── [other files]        # Dashboard pages and layouts
```

## Component Organization

Components in Yamify follow these organizational principles:

1. **Global Components**: Reusable across the entire application (`/src/components/`)
2. **Feature-specific Components**: Used within specific features (`/src/app/[feature]/_components/`)
3. **Page Components**: The main component for a specific route (`/src/app/[route]/page.tsx`)

## Style Organization

Yamify uses a combination of:

- TailwindCSS for utility-based styling
- Global CSS in `src/app/globals.css`
- Component-specific styling when needed

## Data Flow

The application follows a typical Next.js data flow pattern:

1. Data fetching in server components
2. Passing data down to client components as props
3. Client-side state management for interactive elements

For more details on specific aspects of the architecture, see:

- [Data Flow](/architecture/data-flow)
- [Technologies](/architecture/technologies)
- [Design Patterns](/architecture/design-patterns)
